# Partial and Pick

<EpicVideo url="https://www.epicweb.dev/workshops/advanced-typescript/utility-types/partial-pick" />

👨‍💼 Perfect! You've used `Partial` and `Pick` to create a type-safe update
function. Notice how TypeScript prevents you from updating fields that aren't
in `UserUpdate`.

🦉 Combining `Partial` and `Pick` is a common pattern for update operations.
It ensures you can only update specific fields, and all updates are optional.

Next, let's explore `Record` and `Readonly`!
