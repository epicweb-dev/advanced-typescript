# Basic Conditionals

👨‍💼 Let's create conditional types that check and transform based on type
conditions.

🐨 Open <InlineFile file="index.ts" /> and:

1. Create an `IsString<T>` type that returns `true` or `false`
2. Create an `IsArray<T>` type
3. Create a `Flatten<T>` that unwraps arrays
4. Create `NonNullable<T>` from scratch

💰 Conditional types pick one of two results based on `extends`.

📜 [TypeScript Handbook - Conditional Types](https://www.typescriptlang.org/docs/handbook/2/conditional-types.html)

💰 Conditional types can be tricky to understand. Try experimenting in the
[TypeScript Playground](https://www.typescriptlang.org/play) - hover over type
aliases to see how TypeScript evaluates each step of the condition!
