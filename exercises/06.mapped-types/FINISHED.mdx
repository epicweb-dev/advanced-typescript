# Mapped Types

👨‍💼 You now understand how utility types work under the hood!

You learned:

- 🔄 **Iteration** with `[K in keyof T]`
- ✨ **Transformation** of property types
- ➕ **Adding modifiers** like `?` and `readonly`
- ➖ **Removing modifiers** with `-?` and `-readonly`

🦉 You can now create custom utilities:

```ts
// Make all properties nullable
type Nullable<T> = { [K in keyof T]: T[K] | null }

// Make all properties promises
type Promisify<T> = { [K in keyof T]: Promise<T[K]> }

// Make all properties getters
type Getters<T> = { [K in keyof T]: () => T[K] }
```

This is advanced TypeScript, but incredibly powerful for library authors and
complex applications.

Next up: Conditional types—type-level if/else!
