# Module Boundaries

👨‍💼 Our code is mixing concerns. Let's organize it into separate modules with
clear boundaries.

🐨 Reorganize the code:

1. Create a `users` directory with:
   - `types.ts` - User type definitions
   - `api.ts` - User API functions
   - `index.ts` - Barrel export (re-exports everything)

2. Create a `products` directory with:
   - `types.ts` - Product type definitions
   - `api.ts` - Product API functions
   - `index.ts` - Barrel export

3. Update `index.ts` to import from the new modules


📜 [TypeScript Handbook - Modules](https://www.typescriptlang.org/docs/handbook/modules.html)
