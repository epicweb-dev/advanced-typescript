# Typing Async Code

👨‍💼 Excellent! You now understand how TypeScript types async code.

You learned:

- 🔄 **Async functions** return `Promise<T>` automatically
- 🎯 **Type inference** works with async/await
- 🛡️ **Type safety** catches errors at compile time
- 📝 **Explicit types** make code more maintainable

🦉 Remember: When you `await` a Promise, TypeScript unwraps the Promise
type. So `await Promise<string>` becomes `string`.

```ts
const promise: Promise<string> = fetchName()
const name: string = await promise // TypeScript knows this is string
```

Next up: Modules—organizing your code!
